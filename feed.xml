<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://gauthiercler.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://gauthiercler.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-14T12:52:32+00:00</updated><id>https://gauthiercler.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">From templates to neural networks: an intuitive journey on profiled side channel attacks</title><link href="https://gauthiercler.github.io/blog/2026/template-neural-networks-sca/" rel="alternate" type="text/html" title="From templates to neural networks: an intuitive journey on profiled side channel attacks"/><published>2026-07-31T00:00:00+00:00</published><updated>2026-07-31T00:00:00+00:00</updated><id>https://gauthiercler.github.io/blog/2026/template-neural-networks-sca</id><content type="html" xml:base="https://gauthiercler.github.io/blog/2026/template-neural-networks-sca/"><![CDATA[<h1 id="from-templates-to-neural-networks-an-intuitive-journey-on-profiled-side-channel-attacks">From templates to neural networks: an intuitive journey on profiled side channel attacks</h1> <p>Profiled attacks are the strongest scenario for an attacker when performing side channel attacks. Thanks to the access to an open device, leakage characterization can be performed prior to the attack on the closed target device. Since 2002, the standard tool for profiled attacks was the template method. However, since, neural networks have been introduced as a powerful alternative and studied thoroughly.</p> <p>Through this post, we will see the link between templates and neural networks and see that the latter ones are not always the definitive answer for profiled attacks.</p> <p>No deep learning background is required beyond linear algebra and probability.</p> <h2 id="context">Context</h2> <p>Let’s consider an embedded device (a smart card, microcontroller, IOT device, …) running cryptographic routines where a sensitive secret key is manipulated.</p> <p>While the used algorithm is mathematically hardened, the device can leak information physically through side channels, such as power consumption, electromagnetic emanation, timing or temperature.</p> <p>The resulting signal of each execution can be recorded as a trace vector $\mathbf{x}$ of sampled points.</p> <p>The relationship between the obtained trace and the secret is defined through a leakage model, the most standard ones being:</p> <ul> <li>the Identity model, where the signal depends on the exact intermediate value</li> <li>the Hamming weight/distance model, where for CMOS circuits, consumption is tied to bit transitions</li> </ul> <p>Let’s now consider for the sake of simplicity a widely studied cryptographic primitive target, the AES first round S-box output $y = \text{Sbox}(p \oplus k)$ for a single byte, with known plaintext $p$ and secret key $k$. Recovering $y$ allows to recover $k$ since $p$ is known. The search space is a 256 classes (8 bit) classification problem.</p> <p>Theoretically, this could be tackled entirely through a non-profiled attack such as a CPA or MIA, without the need of an open device. However, we focus in this post on the other approach, where prior characterization of the leakage is performed on a clone open device.</p> <p>While less realistic than non profiled ones, profiled attacks are the best scenario for an attacker and the worst for the targeted entity.</p> <p>The profiled classification problem can be defined as follow. Given a set of independent samples $\mathbf{x}$, their associated target classes $y$, and a predictor $f(\mathbf{x})$ which is trained using the data to predict the accurate class label $\hat{y}$ or a probability distribution over all possible labels. This is known as the training phase. This corresponds in our case to the access to the open device, where the attacker can obtain many tuples $(\mathbf{x}, y)$ with $\mathbf{x}$ being acquired signal trace and $y$ the actual value targeted.</p> <p>Then, the fitted model $f$ is applied on traces from the target device to predict the intermediate values of the unknown fixed key. This is known as the matching phase.</p> <p>Note that we assume perfect portability between open and target devices. In practice, between devices portability and measurement differences can affect the matching phase. <d-cite key="bhasin_portability"></d-cite><d-cite key="choudary_portability"></d-cite></p> <p>Now, the question that arises is how to construct an accurate model that maps the leakage observed on a device $A$ to a device $B$.</p> <h2 id="template-attack">Template attack</h2> <p>The template is one of the fundamental approaches for performing profiled attacks.</p> <p>A template can be seen as a profiled classifier.</p> <p>The general form for template attack, as introduced in the original paper <d-cite key="chari_template"></d-cite> is the Quadratic Discriminant Analysis (QDA). The main assumption is the multivariate normality. The template is a deterministic method which models the conditional probability $p(x|k)$ from the data with the multivariate normal density function of dimension $d$:</p> <p>\(p(x|k) = \frac{1}{\sqrt{(2\pi)^d|\Sigma_k|}}\exp{(-\frac{1}{2}(x-\mu_k)^T\Sigma_k^{-1}(x-\mu_k))}\) where $x$ is a vector of dimension $d$ for each trace, $\mu_k$ and $\Sigma_k$ are the mean vector and covariance matrix for class $k$.</p> <p>From there the prediction step uses the discriminant (or score function) for each class guess:</p> \[\delta_k(x) = -\frac{1}{2}\log|\Sigma_k|-\frac{1}{2}(x-\mu_k)^T\Sigma^{-1}_k(x-\mu_k) + \log P(k)\] <aside><p>In practice, if the prior P(k) is uniform (for example 256 classes AES byte), it is often dropped, then the QDA becomes a pure MLE classifier instead of a MAP classifier</p></aside> <p>Then the prediction is the class with the highest score:</p> \[\hat{y} = \arg \max_k \delta_k(x)\] <p>It is also possible to map to the probability simplex $p(x) \in \Delta^{K-1}$ using the softmax function $\sigma : \mathbb{R}^K \rightarrow \Delta^{K-1}$ such that for each class, the probability is:</p> \[p_k(x) = \frac{\exp(\delta_k(x))}{\sum_{j=1}^K\exp(\delta_j(x))}\] <table> <tbody> <tr> <td>This allows to recover the bayesian posterior $p(k</td> <td>x)$.</td> </tr> </tbody> </table> <p>While powerful, the main problem of the QDA method is that it scales poorly. Indeed, many samples are needed for each class to ensure correct covariance matrices estimation and avoid singularities.</p> <p>To reduce the complexity, many works use instead the Linear Discriminant Analysis (LDA), also known as a pooled template <d-cite key="choudary_lda"></d-cite> in the side channel domain. It reduces to a single shared covariance matrix across all classes, so $\Sigma_k = \Sigma$ for all $k$. In other words, the ellipsoid shape of all classes is identical, only shifted by their means. &lt;aside&gt;&lt;p&gt;this is known as Homoscedasticity.&lt;/p&gt;&lt;/aside&gt;</p> <p>Since the covariance matrix is now shared, the quadratic term $x^T\Sigma^{-1}x$ becomes identical to all classes and can be dropped, thus the discriminant becomes:</p> <p>\(\delta_k(x) = x^T\Sigma^{-1}\mu_k - \frac{1}{2}\mu_k^T\Sigma^{-1}\mu_k + \log P(k)\) with the first term being strictly linear to $x$.</p> <p>The above discriminant for LDA can also be reformulated as: \(\delta_k(x) = \mathbf{w}^T_kx + b_k\) with the dot product $\mathbf{w}_k = \Sigma^{-1}\mu_k$ and the bias $b_k = -\frac{1}{2}\mu_k^T\Sigma^{-1}\mu_k + \log P(k)$. We will see later how this relates to neural networks.</p> <p>The assumption of shared covariances for the LDA is consistent for side channel as we expect the measurement noise components to behave similarly accross all classes (in case of first order leakages).</p> <h2 id="limits">Limits</h2> <p>Still, while templates are powerful tool for profiled attacks, they emphasize some limitations.</p> <p>Firstly, their decision boundary is defined and can be limiting. The QDA boundary is a quadratic hypersurface since only the two first statistical moments are used (mean and variance). The LDA on the other side limits to the first order moment expression (since the covariance matrix is shared), and thus the boundary becomes a flat hyperplane.</p> <p>For instance, on a masking countermeasure of order $d$, the lowest statistical moment of leakage is $d+1$, equal to the number of shares. [in univariate case].</p> <p>A workaround for this problem can be to perform some non-linear projections to a subspace where the template can be applied (for example using kernel methods). This is however heavy and complex leakage models are still hard to approximate. Alternatively, the attacker is required to perform recombination of shares beforehand (for example centered product of shares time points).</p> <p>Another limitation of the template for profiled attacks is its poor resistance to jitter on the traces. The template models a multivariate distribution, where each dimension corresponds to realizations of a single temporal point. Therefore precise alignment is necessary for optimal results and to avoid unmatched indices.</p> <p>Finally, template scale poorly when the number of dimensions increases. This is primarily due to cost for the inversion of the covariance matrix ($\mathcal{O(d^3)}$ for the LDA, $\mathcal{O(k \cdot d^3)}$ for the QDA). It is often better to first perform a dimensionality reduction (using a PCA for example) or select only the most relevant point of interests beforehand.</p> <p>Let now see how neural networks come as a relevant alternative against these limitations as they have the ability to learn these steps.</p> <h2 id="neural-networks">Neural networks</h2> <p>As we have seen, templates handle the profiling task by maximizing the likelihood in closed form from the data.</p> <p>Neural networks instead solve the problem through an optimization procedure.</p> <p>A elementary neural network layer consists in the following:</p> \[h = \phi(\mathbf{W}x + b)\] <p>where $\phi$ is an activation function. If $\phi$ is the identity function, it simply consists in the affine transformation, which is equivalent to the LDA reformulation from previous section. Indeed, in this scenario the LDA and neural network spans the same hyperplane family.</p> <p>The strength of neural networks lie in the choice of $\phi$. For example, if we consider the ReLU activation $\phi(x) = \max(0,x)$, the boundary becomes non-linear [relu is one of the fundamental non linear activation for hidden layers. many variants where derived form ReLU (ELU, SELU, GELU, Leaky ReLU). This allows neural networks to be universal function approximator with enough capacity of $\mathbf{W}$.</p> <p>This can then be used as function composition to build layers:</p> \[f(x) = f_L(\cdots f_2(f_1(x)))\] <p>This composition of layers is known as Multilayer Perceptron (MLP).</p> <p>It is possible to imagine the neural network composition as a straight line bended at each activation. The following figure illustrates the boundary difference between the LDA, QDA and neural network:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/boundary-480.webp 480w,/assets/img/boundary-800.webp 800w,/assets/img/boundary-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/boundary.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Without non linear activations, the model would simply be a composition of affine maps, and would not be able to model higher degrees polynomial. For the side channel task, it means that if the neural network capacity is sufficient (enough layers and neurons per layers), it can theoretically models/represents masked leakage of any order with enough leaky data examples. [Masking remains really effective since the number of traces needed grows exponentially as the number of shares increases. For high order masking, some help is often needed such as data augmentation or precomputed products.</p> <p>The prediction obtained through this composition (the forward pass) is then compared to the expected value, using a loss (or cost function) $\mathcal{L}$ which define the model objective.</p> <p>For instance, in a classification problem, the cross entropy loss is typically used:</p> \[\mathcal{L} = -\sum_{k=1}^K y_k \log p_k\] <p>between the expected $y$ (one hot encoded labels) and predicted probability distribution $p$ (using softmax as last layer activation). Minimization of the categorical cross-entropy with one-hot labels is equivalent to maximizing the conditional log-likelihood like the template fitting.</p> <p>The optimization procedure consists in finding weights $\mathbf{W}$ that minimize this loss. Gradients are propagated from the loss back to the first layer using the chain rule backpropagation (the backward pass).</p> <p>This forward/backward cycle is repeated several times on batches of data until convergence is reached (a local minima).</p> <p>Unlike the template which output is deterministic, the neural network convergence results are sensitive to the weights initialization.</p> <p>As long as the whole architecture is differentiable, neural networks can be applied to various tasks with defined objectives. Additionally, we saw now the fully connected block (all neurons are interconnected), but other blocks are possible.</p> <p>At this point we see that neural networks are overcoming template limitations for higher order leakage extraction.</p> <p>Still, this architecture is likely to struggle against jitter or desynchronization countermeasures like the template. For optimal efficiency, alignment is still recommended.</p> <p>Additionally, the training for the neural network is a stochastic process and thus partially depends on the weights random initialization.</p> <h2 id="convolutions">Convolutions</h2> <p>We saw that MLP can model non-linear high order leakages, but like the template they still require aligned temporal points of each sub-leakage for optimal exploitation.</p> <p>It is possible to learn inside the model the feature extraction step. This can be performed by using convolution layers. This is analogous to convolution used in signal processing.</p> <p>This can be seen as a pre processing step, but it is powerful as it is done automatically during the training.</p> <p>Essentially, convolutions for neural networks are set of independent filters which parameters are learnt. these filters are slid along the trace like a moving operator with the matrix being the learned convolution weights.</p> <p>Additionally, in their most basic form, these convolutions are completed with pooling layers, which are used to perform some down sampling. Average or Max pooling layers are usually used.</p> <aside><p>more complex convolutions blocks are also used, such as residual blocks, unet,...</p></aside> <p>The output of these blocks is then flattened and usually plugged into the logical part of the network which consists in an MLP classification head. [keep in mind that unlimited architecture variants are possible, this is just for explanation purposes].</p> <p>Extensive research has been performed in the side channel domain since the introduction of neural networks for profiled attacks (2016), Some works aimed at proposing methodology for selecting the optimal architecture parameters <d-cite key="zaid_methodology"></d-cite>, visualisation and explainability <d-cite key="masure_gradient"></d-cite>.</p> <p>It is also relevant to note that convolutions can have a recombination effect if multiple shares are manipulated at close temporal intervals.</p> <p>This allows a fully differentiable self-contained preprocessing/filtering + feature extraction + classifier head pipeline.</p> <h2 id="accuracy-is-not-the-key">Accuracy is not the key</h2> <p>In machine learning field, for general purpose classification accuracy is almost always the considered metric.</p> <p>However, for side channel profiled attacks with traces accumulation, thats not necessarily the case.</p> <p>As the predictions are combined among several traces, the goal is to obtain a decrease of entropy. That is why the rank of the correct key (argsort) is seen as a better indicator of performance. Even an average rank slightly higher to random can be sufficient for key recovery by accumulating enough traces. &lt;aside&gt;&lt;p&gt;If the rank of the correct value is always 2, the resulting accuracy is equal to zero.&lt;/p&gt;&lt;/aside&gt;</p> <p>It mainly depends on what is the goal and the attack path.</p> <p>As introduced previously , if we target an AES encryption and we try to recover the unknown fixed key on the target device, we can perform scores accumulation, analogously to the CPA, which is accumulating traces and computing the Pearson coefficient for each guess.</p> <p>We can do it in similar fashion here. As described above for both template and neural network, we obtain a probability distribution over all class guesses, for each trace.</p> <p>From there, several methods can be used to combine these probabilities. One way can be to simply multiply each guess value across several realizations. After accumulation, the largest resulting probability is the most probable candidate.</p> <aside><p>For numerical stability on large set of accumulations, we often do the sum of log’s probabilities instead&lt;/aside&gt;<p> Others smarter approaches exist to combine probabilities, such as histogram exploration or belief propagation (for several intermediate values). For neural networks, it is a good habit to monitor the average rank of the correct class (on batches) during the training. This allows to see if the model is able to learn on the data or not. The rest of the attack is almost identical to the CPA. Instead of working with Pearson correlation values, it now uses probabilities. The decrease in guessing entropy (average rank on several attacks) during accumulation until the remaining key space is small enough to brute-force. ## Conclusion In this post, we covered templates and neural networks as tool for profiled side channel attacks. We saw how the latter one can overcome the limitations of the former one, using different paradigms. Nevertheless, it is important to keep in mind that templates remain competitive for profiled side channel attacks, especially for simpler leakage models. Unlike Neural networks, they do not require extensive architecture or parameters tuning. Especially consider the LDA if traces are limited. Additionally, working with neural networks for profiled attacks is not as straightforward as with templates. It requires some expertise for model training and architecture tuning. Here is a table which summarizes briefly the main differences between the two approaches: | | Template (LDA/QDA) | Neural network (MLP/CNN) | |:--|:--|:--| | Model type | Generative, $p(x\mid y)$ | Discriminative, $p(y\mid x)$ | | Estimation | Closed-form | Gradient descent | | Parametric | No | Architecture + hyper-parameters | | Decision boundary | Hyperplane / quadratic | Arbitrary (depends on capacity) | | Reproducibility | Deterministic | Stochastic, sensitive to initialization | | Higher-order recombination | Manual, beforehand | Learned | | Feature selection | POI / PCA needed | Automated | | Desync robustness | Needs alignment | Pooling + augmentation | | Ease of use | Straightforward | Requires expertise | </p></p></aside>]]></content><author><name></name></author><category term="side-channel,"/><category term="neural"/><category term="networks,"/><category term="template"/><summary type="html"><![CDATA[A post building from template attacks and their limitations to the neural networks that have driven the field in recent years]]></summary></entry><entry><title type="html">Adversarial Attacks</title><link href="https://gauthiercler.github.io/blog/2021/adversarial-attacks/" rel="alternate" type="text/html" title="Adversarial Attacks"/><published>2021-05-22T00:00:00+00:00</published><updated>2021-05-22T00:00:00+00:00</updated><id>https://gauthiercler.github.io/blog/2021/adversarial-attacks</id><content type="html" xml:base="https://gauthiercler.github.io/blog/2021/adversarial-attacks/"><![CDATA[<p>Adversarial attacks on machine learning is a wide domain of interest. In this first, we cover white box adversarial attacks using the gradient sign method. In a white box setting, the attacker has access to all the information used for training the model, its weights and parameters, along the used datasets for training.</p> <h3 id="fgsm">FGSM</h3> <p>The Fast Gradient Sign Method (FGSM) is a straightforward approach proposed in <d-cite key="goodfellow2015explainingharnessingadversarialexamples"></d-cite> to generate adversarial sample from a trained model. Given the model parameters $\boldsymbol{\theta}$, some input samples $\boldsymbol{x}$ and associated ground truth label $y$, the general objective that the model aims at minimizing during training is defined as:</p> \[J(\boldsymbol{\theta}, \boldsymbol{x}, y)\] <p>The adversarial perturbation is given as:</p> \[\boldsymbol{\eta} = \epsilon \text{sign}(\nabla_{\boldsymbol{x}} J(\boldsymbol{\theta}, \boldsymbol{x}, y)).\] <p>By taking the gradient of the loss w.r.t to $\boldsymbol{x}$, it provides the direction in which the input $\boldsymbol{x}$ should change in order to maximize the loss. Thus, a new generated adversarial sample can simply be generated as $\boldsymbol{x’} = \boldsymbol{x} + \boldsymbol{\eta}$. Given a small value of $\epsilon$, a new generated sample cannot be distinguished by the human eye from a real one, but is sufficient to deceive the model. In other words, a slight alteration of the input image pixels causes a consequent change in the model embedding.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/fgsm-480.webp 480w,/assets/img/fgsm-800.webp 800w,/assets/img/fgsm-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/fgsm.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Image from <d-cite key="goodfellow2015explainingharnessingadversarialexamples"></d-cite>.</figcaption> </figure> <p>However, this approach only causes the model to missclassify samples. Instead, an variant can be used to target a specific class to be predicted by the model, maximizing $p(\hat{y}|\boldsymbol{x})$, where $\hat{y}$ corresponds to the label of the targeted class. Previous perturbation now becomes:</p> \[\boldsymbol{\eta} = \epsilon \text{sign}(\nabla_{\boldsymbol{x}} J(\boldsymbol{\theta}, \boldsymbol{x}, \hat{y})).\] <p>This time, as we aim at going in the direction that minimize the loss (instead of maximizing it) of the target class $\hat{y}$, the adversarial sample becomes $\boldsymbol{x’} = \boldsymbol{x} - \boldsymbol{\eta}$. The choice of the target class can be motivated by a specific attack strategy (for example predict dogs as cats). In <d-cite key="kurakin2017adversarialexamplesphysicalworld"></d-cite>, authors suggest to choose the least likely class based on the trained model prediction:</p> \[\hat{y} = \underset{y}{\arg\min} \{p(y|\boldsymbol{x})\}\] <p>This is motivated by the fact that least likely predicted class in is higly dissimilar to the actual true class. This can be used to maximize the model misclassification.</p> <p>This adversarial process can also be applied iteratively with a small step size.</p> \[\boldsymbol{x'}_{n + 1} = \boldsymbol{x'}_{n} - \epsilon \text{sign}(\nabla_{\boldsymbol{x}_n} J(\boldsymbol{\theta}, \boldsymbol{x}, \hat{y}))\] <aside><p>Note that clipping may be necessary depending on the data used.</p></aside> <p>with $\boldsymbol{x’}_{0} = \boldsymbol{x}$. This latter approach is more effective than the former one step generation as it produce smaller perturbations and is less destructive.</p> <h3 id="adversarial-training">Adversarial training</h3> <p>This adversarial generation method can also be used as a regularization for training more robust models. In its simplest way, adversarial training can be formulated as:</p> \[\tilde{J}(\boldsymbol{\theta}, \boldsymbol{x}, y) = \alpha J(\boldsymbol{\theta}, \boldsymbol{x}, y) + (1 - \alpha) \epsilon \text{sign}(\nabla_{\boldsymbol{x}} J(\boldsymbol{\theta}, \boldsymbol{x}, y)).\] <p>with hyperparameters $\epsilon$ and $\alpha$ acting on the perturbation level and the regularization balancing between the two terms respectively.</p>]]></content><author><name></name></author><category term="adversarial"/><summary type="html"><![CDATA[Adversarial attacks on machine learning is a wide domain of interest. In this first, we cover white box adversarial attacks using the gradient sign method. In a white box setting, the attacker has access to all the information used for training the model, its weights and parameters, along the used datasets for training.]]></summary></entry></feed>